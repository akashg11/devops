#include <stdio.h>
int main() {

    int number1=14, number2=29, sum;

    // calculating sum
    sum = number1 + number2;
    printf("qwertyu");
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}

